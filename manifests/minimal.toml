# Minimal sandbox: filesystem, search, JSON, and CSV
# Build: cpsl build -t minimal -f manifests/minimal.toml

[sandbox]
name = "minimal"

[modules]
fs = true
grep = { provider = "ripgrep" }
json = true
csv = true
